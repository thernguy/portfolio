import {FC} from 'react';

interface pageProps {
  // Define your component props here
}

const page: FC<pageProps> = (props) => {
  return (
    <div>
      contact page
    </div>
  );
};

export default page;    