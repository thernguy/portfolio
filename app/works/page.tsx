import {FC} from 'react';

interface pageProps {
  // Define your component props here
}

const page: FC<pageProps> = (props) => {
  return (
    <div>
      work page
    </div>
  );
};

export default page;